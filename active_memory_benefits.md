# Active Memory — 附加收益与 KV Cache 影响分析

## 目录

- [1. 对 KV Cache 的影响](#1-对-kv-cache-的影响)
- [2. 十项附加收益](#2-十项附加收益)
- [3. 总结](#3-总结)

---

## 1. 对 KV Cache 的影响

### 存储结构变化

```
标准 KV Cache (每层):
  K: [B, 1, T, D]     ← Key 矩阵
  V: [B, 1, T, D]     ← Value 矩阵
  总计: 2 × B × T × D

Active Memory KV Cache (每层):
  K: [B, 1, T, D]     ← Key 矩阵 (不变)
  V: [B, 1, T, D]     ← Value 矩阵 (不变)
  A: [B, H, T]         ← 激活度 (新增)
  总计: 2 × B × T × D + B × H × T
```

### 显存开销

| 模型规模 | 标准 KV Cache | 激活度增量 | 增幅 |
|---|---|---|---|
| 110M (本项目, 8层, H=8, D=96, T=2048) | 3,072 KB | +512 KB | +16.7% |
| 70B (80层, H=64, D=128, T=128K) | ~40 GB | ~2.5 GB | +6.25% |

激活度只是 [B, H, T]（每 token 每 head 一个标量），比 KV 的 [B, H, T, D] 少了 D 倍。**开销可接受。**

### 计算开销

| 操作 | 标准 MLA | Active Memory | 说明 |
|---|---|---|---|
| 注意力计算 | SDPA (fused) | 手动 matmul + softmax | 需要在 softmax 前修改分数 |
| 激活度衰减 | 无 | O(H × T) 逐元素乘 | 很快 |
| 扩散卷积 | 无 | O(H × T × W) 1D conv | W=5，很快 |
| 线索加成 | 无 | O(H × S × T) softmax + mean | 复用 attn scores |
| 分数调制 | 无 | O(H × S × T) 加法 | 很快 |

**最大性能损失来自丢失 SDPA fused kernel**（预计 30-50%），而非激活度计算本身。

### 与 KV 优化技术的兼容性

| 优化技术 | 兼容性 | 说明 |
|---|---|---|
| Prefix Caching | ✅ 兼容 | 共享前缀激活度也共享，后续 copy-on-write |
| KV 量化 (INT4/8) | ✅ 兼容 | KV 照常量化，激活度保持 float |
| KV Eviction (H2O等) | ⚠️ 需协调 | 驱逐 KV 时同步驱逐激活度；但激活度本身可作为更好的驱逐指标 |
| GQA/MQA | ✅ 兼容 | 激活度 per-head，和 KV sharing 解耦 |
| MLA 低秩压缩 | ✅ 兼容 | 激活度加在 score 上，和 KV 表示方式无关 |
| Sliding Window | ⚠️ 需注意 | 窗口外 KV 丢弃时激活度信息也丢失 |
| PagedAttention (vLLM) | ⚠️ 需改动 | 需要额外管理激活度的分页 |

---

## 2. 十项附加收益

### 收益 1: 天然的注意力可解释性

标准 attention 只能看到 softmax 后的权重——知道"模型在看哪"，不知道"为什么"。

Active Memory 可以分解注意力来源：

$$\alpha_i = \text{softmax}\left(\underbrace{\frac{q \cdot k_i^T}{\sqrt{d}}}_{\text{内容匹配贡献}} + \underbrace{\lambda \cdot a_i}_{\text{联想激活贡献}}\right)$$

可以量化"模型注意到某个 token，有多少是因为内容匹配，有多少是因为联想激活"。

**实际用途**：调试幻觉时，检查错误输出是因为 query-key 匹配错了，还是因为不相关记忆的激活度异常"侵入"了。

---

### 收益 2: 更智能的上下文窗口管理 / KV Eviction

```
标准方法:
  - 滑动窗口: 丢最早的 → 可能丢关键信息
  - H2O: 丢注意力累积最低的 → 只看历史，不考虑未来
  - StreamingLLM: 保留开头 + 最近 → 经验规则

Active Memory:
  每个 token 有实时更新的重要度 a_i
  → 低激活 token 被压缩或丢弃
  → 高激活 token 精确保留
  → 扩散激活保护了"和重要 token 关联"的 token（不会被误丢）
```

激活度是**动态的、全局的、有上下文的**，优于任何基于历史统计的 eviction 策略。

---

### 收益 3: 自适应的注意力锐度

标准 attention 的锐度由温度（√d）固定。Active Memory 的 λ·a_i 偏置相当于给不同 token 不同的温度：

```
高激活 token: score 被抬高 → 更容易脱颖而出 → 注意力更集中
低激活 token: score 不变 → 需要更强匹配才能被选中

效果: 粗注意力（先看"明星" token）+ 细注意力（再匹配细节）
      两阶段检索，在一个 softmax 里完成
```

类似人类阅读：先扫关键词，再精读细节。

---

### 收益 4: 隐式的语义分块（Chunk 感知）

扩散激活让相邻 token 激活度趋于相似，自然形成"高原"和"低谷"：

```
Token:       我  今天  去了  公司  ，  开了  一个  很  长  的  会  。  然后  ...
激活度:      0.3  0.8   0.7  0.9  0.2  0.6   0.5  0.4 0.4 0.2 0.5 0.1  0.7  ...
               ╰─────── 高原 ──────╯  ╰──── 中等 ────╯        ╰── 低谷 ──╯

自然分段:    [我今天去了公司]  [开了一个很长的会]  [然后...]
```

**无需显式分块机制，扩散激活自动产生语义相关的"记忆块"。** 与人类的事件分段（Event Segmentation）一致——你记得的是"去了公司""开了个会"这样的事件，不是每一秒。

KV eviction 时可按语义块整体丢弃，而非逐 token 丢弃。

---

### 收益 5: 缓解 "Lost in the Middle"

长上下文中排在中间位置的信息被模型忽略——这是当前 LLM 的已知缺陷。

```
标准 attention:
  示例1 [最前面] → 距 query 最远 → 注意力最弱
  示例2 [中间]   → 距离中等       → 被忽略!
  示例3 [最后]   → 距 query 最近 → 注意力最强

Active Memory:
  示例1: 距离远，但通过扩散保持激活
  示例2: 和示例1、3 互相扩散激活
  示例3: 最近，激活最高
  → 所有示例激活度更均匀 → 中间信息不再被遗漏
```

激活度不纯粹依赖位置——扩散机制让重要信息无论在哪都能保持足够的影响力。

---

### 收益 6: 生成多样性的精细控制

标准方法用 temperature / top-p 统一控制。Active Memory 提供三个语义清晰的旋钮：

| 旋钮 | 调大效果 | 类比 |
|---|---|---|
| λ (激活度权重) | 更依赖高激活记忆 → 保守、聚焦已知 | "集中精力回忆" |
| ε (噪声) | 更多随机激活 → 发散、意外联想 | "放空思绪，灵感涌现" |
| β (线索加成) | 更依赖 query 匹配 → 紧扣问题 | "认真审题" |

可以组合：**"认真审题（高 β）但允许偶尔联想（中等 ε）"**。比 temperature 一个旋钮精细得多。

---

### 收益 7: Few-Shot 学习增强

Few-shot prompt 中的示例需要保持足够影响力。Active Memory 的扩散让所有示例互相激活，不会因为位置靠前就被"遗忘"。

这和收益 5（Lost in the Middle）直接相关——few-shot 是受该问题影响最严重的场景之一。

---

### 收益 8: MoE 上下文感知路由

```
当前 MoE 路由:
  gate(x) → 只看当前 token → 选专家

有激活度的路由:
  gate(x, context_activation) → 知道"记忆中什么是活跃的" → 选更合适的专家
```

如果记忆中数学相关 token 激活度高，MoE 可以倾向选择逻辑推理专家。需要小改动，但给专家选择加了上下文感知。

---

### 收益 9: 隐式课程学习

```
训练初期:
  γ、λ 还没学好 → 激活度接近均匀 → 行为≈标准 attention
  → 先学基本语言模型能力

训练中期:
  γ、λ 开始分化 → 某些 head 开始依赖联想
  → 在基础能力上叠加联想

训练后期:
  γ、λ 收敛 → 每个 head 有清晰角色
  → 有的 head 做精确匹配，有的做联想
```

模型从简单（标准 attention）自然过渡到复杂（联想 attention），无需手动设计课程。

---

### 收益 10: 长对话话题追踪

```
轮次1: 聊天气  ← 天气 token 激活度高
轮次2: 聊工作  ← 工作 token 高，天气衰减
轮次3: 聊晚餐  ← 晚餐 token 高
轮次4: "对了，刚才说的天气..."
       ← "天气"一词通过扩散，瞬间重新激活轮次1整个话题簇

标准 attention: 需要 query 精确匹配到轮次1的 token
Active Memory: 一个关键词激活整个话题块
```

就像人类对话中的"哦对了，刚才说的那个..."——一个词就能把整段之前的讨论重新拉回来。

---

## 3. 总结

### 收益全景

| 收益 | 类型 | 额外成本 |
|---|---|---|
| 注意力可解释性 | 免费获得 | 零 |
| 更智能的 KV eviction | 免费获得 | 零 |
| 自适应注意力锐度 | 免费获得 | 零 |
| 隐式语义分块 | 免费获得 | 零 |
| 缓解 Lost in the Middle | 免费获得 | 零 |
| 生成多样性精细控制 | 推理时可调 | 零 |
| Few-Shot 学习增强 | 免费获得 | 零 |
| MoE 上下文感知路由 | 需要小改动 | 很小 |
| 隐式课程学习 | 免费获得 | 零 |
| 长对话话题追踪 | 免费获得 | 零 |

### 核心洞察

> 大多数收益是"免费"的——只要有了 per-token 的动态激活度，这些能力自然涌现。不需要为每个能力单独设计机制。
>
> 这正是好的架构设计的标志：**一个简单的改动，产生多个正交的收益。** 就像 attention 本身一个机制同时解决了对齐、长距离依赖、并行化。Active Memory 的激活度也是类似的"杠杆点"——一个标量，撬动了可解释性、记忆管理、生成控制等多个维度。

### KV Cache 影响总结

| 维度 | 影响 | 严重程度 |
|---|---|---|
| 显存增加 | +6-17% | 低 |
| 计算变慢 | 30-50%（丢失 SDPA） | 中 |
| 推理兼容 | 逐 token 更新激活度 | 低 |
| vLLM 集成 | 需改 PagedAttention | 中 |
| KV 优化兼容 | 大部分兼容 | 低 |
| 意外改善 | 激活度→更好的 eviction | 正面 |

---

*Active Memory Attention — nanowhale project, May 2026*
